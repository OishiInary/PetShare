<div class="row bg-light border text-center" style="background-color: #EECAD5; border-radius: 50px;">
<h3 class="text-center mx-auto">SAVE活動<%= image_tag "/nekotoaruku_touka.gif", size: "60x60" %></h3></div>
<div class="row">
  <p>里親を探しているひとは里子募集の欄から気になったユーザーに連絡をしてみよう。</p>
<p>里子を募集している人は里親募集中のペット一覧から気になる子を探しその飼い主に連絡をしてみよう。</p>
</div>
<div class="container text-center">
  <div class="row">
    <div class="col-6">
      <h4>里子募集しているユーザー<%= image_tag "/agepiyo.gif", size: "60x60" %></h4>
      <table class="table border" >
        <thead>
          <tr>
            <th>名前</th><th>性別</th><th>ファミリー数</th>
          </tr>
        </thead>
        <tdoby>
          <% @can_users.each do |user| %>
          <tr><td style="padding: auto; vertical-align:middle"><%= link_to user_path(user) do %><%= user.name %><% end %></td>
          <td style="padding: auto; vertical-align:middle"><%= user.gender_i18n %></td>
          <td style="padding: auto; vertical-align:middle"><%= user.pets.count %>匹</td></tr>
          <% end %>
        </tdoby>
      </table>
      <%= paginate @can_users,theme: 'bootstrap-5' %>
    </div>
    <div class="col-6">
      <h4>里親募集しているペット<%= image_tag "/need_help.png", size: "50x50" %></h4>
      <table class="table border">
        <thead>
          <tr>
            <th>写真</th><th>名前</th><th>年齢</th><th>性別</th>
          </tr>
        </thead>
        <tbody>
          <% @need_pets.each do |pet| %>
              <td><%= link_to pet_path(pet) do %><%= image_tag pet.image, size: "100x100" %><% end %></td>
              <td style="padding: auto; vertical-align:middle"><%= pet.name %></td>
            <td style="padding: auto; vertical-align:middle"><%= pet.age %>才</td>
            <td style="padding: auto; vertical-align:middle"><%= pet.gender_i18n %></td>
          <% end %>
        </tbody>
      </table>
       <%= paginate @need_pets,theme: 'bootstrap-5' %>
    </div>
  </div>
</div>